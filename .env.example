# Supabase (required for auth and resume storage)
# Get these from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Gemini API key is NOT set here. Set it in Supabase Edge Function secrets:
# Dashboard → Project Settings → Edge Functions → Secrets
# Add: GEMINI_API_KEY, optionally GEMINI_MODEL (e.g. gemini-2.5-flash, gemini-2.5-pro, gemini-2.5-flash-lite)
